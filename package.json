{
  "name": "ai-json-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Open standard and tooling for ai.json project contracts.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @ai-json-spec/core build && pnpm --filter @ai-json-spec/cli build",
    "changeset": "changeset",
    "changeset:publish": "changeset publish --provenance",
    "changeset:version": "changeset version",
    "clean": "pnpm -r exec rm -rf dist tsconfig.tsbuildinfo",
    "format": "oxfmt README.md CONTRIBUTING.md AGENTS.md CHANGELOG.md SECURITY.md ROADMAP.md specification schema examples packages fixtures docs scripts action.yml package.json tsconfig.json tsconfig.base.json vitest.config.ts .changeset .github/workflows --write",
    "format:check": "oxfmt README.md CONTRIBUTING.md AGENTS.md CHANGELOG.md SECURITY.md ROADMAP.md specification schema examples packages fixtures docs scripts action.yml package.json tsconfig.json tsconfig.base.json vitest.config.ts .changeset .github/workflows --check",
    "lint": "oxlint packages scripts",
    "pack:test": "node scripts/pack-smoke-test.mjs",
    "test": "vitest run",
    "typecheck": "tsc -b tsconfig.json --pretty false"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0",
    "@types/node": "^24.0.0",
    "ajv": "^8.20.0",
    "oxfmt": "latest",
    "oxlint": "latest",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.0.0"
}
