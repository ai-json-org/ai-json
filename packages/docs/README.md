# @ai-json-spec/docs

Private PreactPress site for [ai-json.org](https://ai-json.org).

```sh
pnpm docs:dev
pnpm docs:build
pnpm docs:preview
```

The canonical schema is copied from `schema/v1.json` into the build output as `/schema/v1.json` during `preactpress build`.
