# Changesets

Use Changesets to prepare npm releases for `@ai-json-spec/core` and `@ai-json-spec/cli`.

```sh
pnpm changeset
pnpm changeset version
pnpm changeset publish --provenance
```

Do not publish from local machines unless the release checklist explicitly allows it. The preferred path is the GitHub release workflow.
